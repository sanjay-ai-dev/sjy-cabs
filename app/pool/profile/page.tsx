import { ProfileView } from '@/components/pool/profile-view';

export const metadata = {
  title: 'Profile — DailyCab Pool',
  description: 'Your details and safety preferences for community carpooling.',
};

export default function PoolProfilePage() {
  return <ProfileView />;
}
